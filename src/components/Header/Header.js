import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div className="header">
            <p>leadsquared</p>
        </div>
    );
};

export default Header;
